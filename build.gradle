ext {
    JAVA_VERION = 17;
}

subprojects {
    apply plugin: 'java-library'

    repositories {
        mavenCentral()
    }

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(JAVA_VERION)
        }
    }
}